name: hello-world

on: push

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
      - name: check out repository code
        usae: actions/checkout@v4
      - run: echo "$(cat hello_wirkd.txt)"
        
